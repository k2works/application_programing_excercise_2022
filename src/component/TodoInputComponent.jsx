import React from "react";

export const TodoInputComponent = () => {
  return (
    <input
      id="js-form-input"
      type="text"
      placeholder="What need to be done?"
      autocomplete="off"
    ></input>
  );
};
